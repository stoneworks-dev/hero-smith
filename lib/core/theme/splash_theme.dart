import 'package:flutter/material.dart';

/// Branding colors for the splash screen.
class SplashTheme {
  SplashTheme._();

  static const Color background = Color(0xFF1E1E1E);
  static const Color accent = Color(0xFFE6A33E);
}
