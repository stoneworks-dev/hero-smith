import 'package:flutter/material.dart';

/// Shared surfaces and accents for heroic resource/psi widgets.
class HeroicResourceTheme {
  HeroicResourceTheme._();

  static const Color surface = Color(0xFF1E1E2E);
  static const Color panel = Color(0xFF252535);
  static const Color alertSurface = Color(0xFF2E1E1E);
}
